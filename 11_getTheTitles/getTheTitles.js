const getTheTitles = (books) => {
    let bookValues = books.map(book => book.title) // loop over book.titles
    return bookValues;    
};

// Do not edit below this line
module.exports = getTheTitles;
